# .NET Template Feature

## Cel

Szablon dostarcza startowy backend .NET z CI, testami jednostkowymi i testami architektury.

## Quality Gates

- `dotnet build`
- `dotnet test`
- NetArchTest rules

## PDLC Notes

Agent kodujący musi aktualizować `context.md`, jeśli zmienia kontrakty API, zależności lub komendy testów.
